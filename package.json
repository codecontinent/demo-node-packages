{
  "private": true,
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "test": "jest",
    "compile": "npm run compile -ws"
  },
  "workspaces": [
    "./packages/*"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^20.3.1",
    "jest": "^29.5.0",
    "rimraff": "^0.0.1-security",
    "ts-jest": "^29.1.0",
    "typescript": "^5.1.3"
  }
}
